{
  "scripts": {
    "desktop:dev": "npm --prefix apps/desktop run desktop:dev",
    "desktop:build": "npm --prefix apps/desktop run desktop:build",
    "desktop:web-build": "npm --prefix apps/desktop run build"
  }
}
